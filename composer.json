{
    "name": "difilal/public-html",
    "description": "PHP unit test configuration for Jenkins",
    "type": "project",
    "require-dev": {
        "phpunit/phpunit": "^9.6"
    },
    "scripts": {
        "test:php": "phpunit --configuration phpunit.xml"
    },
    "config": {
        "sort-packages": true
    }
}
